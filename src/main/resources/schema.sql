create table percelInfo (
  waybillNum VARCHAR(64) PRIMARY KEY,
  username VARCHAR(64) NOT NULL,
  telphone VARCHAR(64) NOT NULL,
  weight VARCHAR(64) NOT NULL,
  state VARCHAR(64),
  appointmentTime VARCHAR(64),
);